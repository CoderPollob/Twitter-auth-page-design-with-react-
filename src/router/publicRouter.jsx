import Auth from "../pages/auth/Auth";

// Create PubliceRouter
const publiceRouter = [
  {
    path: "/",
    element: <Auth />,
  },
];

// Export default Publice router
export default publiceRouter;
